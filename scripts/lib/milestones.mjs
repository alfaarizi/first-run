// Tasklet's activation milestones in funnel order, shared so the labeler and
// the verifier accept the same catalog.

export const MILESTONES = ['task_created', 'task_completed', 'completed_tasks_cleared']
